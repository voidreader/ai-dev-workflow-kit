default_platform(:android)

PACKAGE = "__PACKAGE__"
JSON_KEY = File.expand_path("play-store-key.json", __dir__)

platform :android do
  desc "Upload only the Play store listing metadata (no binary, no images)"
  lane :metadata do
    upload_to_play_store(
      package_name: PACKAGE,
      json_key: JSON_KEY,
      skip_upload_aab: true,
      skip_upload_apk: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
      skip_upload_changelogs: true,
    )
  end

  desc "Upload Play store graphics — screenshots, icon, feature graphic"
  lane :images do
    # 주의: skip_upload_metadata를 켜지 말 것 — 고해상도 아이콘과 피처 그래픽은
    # 리스팅(metadata) 업로드 시 함께 첨부된다. 스크린샷만 독립적으로 업로드되며,
    # 텍스트는 다시 전송된다(멱등 — 동일 내용).
    upload_to_play_store(
      package_name: PACKAGE,
      json_key: JSON_KEY,
      skip_upload_aab: true,
      skip_upload_apk: true,
      skip_upload_changelogs: true,
    )
  end

  desc "Set the release notes (changelog) on the production draft release"
  lane :release_notes do
    upload_to_play_store(
      package_name: PACKAGE,
      json_key: JSON_KEY,
      track: "production",
      version_code: 1,
      release_status: "draft", # 앱이 미게시 상태 → 드래프트를 드래프트로 유지
      skip_upload_aab: true,
      skip_upload_apk: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
      skip_upload_changelogs: false,
    )
  end

  desc "Upload the signed AAB to the internal track + metadata"
  lane :internal do
    upload_to_play_store(
      package_name: PACKAGE,
      json_key: JSON_KEY,
      track: "internal",
      aab: "../build/app/outputs/bundle/release/app-release.aab",
      skip_upload_images: true,
      skip_upload_screenshots: true,
    )
  end
end
